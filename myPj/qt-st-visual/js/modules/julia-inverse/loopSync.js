/*
 * Legacy helper for toggling Run/Pause buttons. The current
 * implementation handles this within other modules.  The code
 * is kept here for reference only.
 */

/*
export function syncLoopButtons(loopCtrl) {
        const runBtn   = document.getElementById('btn-run');
        const pauseBtn = document.getElementById('btn-pause');
        if (runBtn && pauseBtn) {
          runBtn.disabled   = loopCtrl.isRunning;
          pauseBtn.disabled = !loopCtrl.isRunning;
        }
  }
*/
  