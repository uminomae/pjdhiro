/*
 * Color mapping utility kept for reference. The 3D code currently
 * uses a different coloring approach, so this function is unused.
 */

/*
import * as THREE from 'three';

export function mapValueToColor(value, maxValue) {
  const ratio = Math.min(Math.max(value / maxValue, 0), 1);
  const hue = 240 - 240 * ratio;
  return new THREE.Color(`hsl(${hue}, 100%, 50%)`);
}
*/
