[trend]
# CSVパス（空なら --csv または --demo を使用）
csv =
# 終値列名
close_col = close
# 通貨（pip換算のため）: JPY or OTHER
currency = JPY
# 判定パラメータ
len_slow = 200
slope_lb = 5
min_slope = 10
# プロット表示の自動クローズ秒（0で手動）
show_seconds = 0


[params]
# 既存のSMAテスト互換のための後方互換キー（存在すればfallbackで使います）
csv =
close_col = close
show_seconds = 0
