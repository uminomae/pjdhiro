//@version=6
library("pjdhiro_bb_v6")

// ボリンジャーバンド： [basis, upper, lower]
export bb(float src, int length, float mult) =>
    int len = math.max(length, 1)
    float basis = ta.sma(src, len)
    float dev   = mult * ta.stdev(src, len)
    [basis, basis + dev, basis - dev]

